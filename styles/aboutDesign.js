import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
    },
    header: {
        backgroundColor:'#fff',
        alignItems:'center',
        padding:5
    }
})

export default styles