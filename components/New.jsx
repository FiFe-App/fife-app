
import { ActivityIndicator, SafeAreaView, Text, View, Pressable, Button, Image, StyleSheet } from 'react-native';

export const New = ({ navigation, route }) => {
    return (
        <View>

        </View>
    )
}